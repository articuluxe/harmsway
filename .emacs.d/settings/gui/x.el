;; -*- Mode: Emacs-Lisp -*-
;;
;;

(tool-bar-mode -1)
(scroll-bar-mode -1)

(setq initial-frame-alist
      '(
        (top . 5) (left . 5) (height . 70) (width . 95)
        ))
(setq default-frame-alist
      '(
        (cursor-type . (bar . 2))
        (height . 70) (width . 95)
        ))
