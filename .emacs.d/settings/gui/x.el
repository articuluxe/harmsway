;; -*- Mode: Emacs-Lisp -*-
;;
;;

(setq initial-frame-alist
      '(
        (top . 5) (left . 5) (height . 55) (width . 80)
        ))
(setq default-frame-alist
      '(
        (cursor-type . (bar . 2))
        (height . 55) (width . 80)
        ))
