# -*- Mode: org -*-

auto-complete: add key words for dos mode, cmake mode
               c-headers: stop completing if file exists; "."

windmove: wrap

ace-popup-menu: not called under window-system-p (e.g. flyspell)

ispell: custom dictionaries

check-bashisms: flycheck checker error, unknown /usr/bin/env

etags: display source file

see `grep-host-defaults-alist' in grep.el; opening a remote file, grepping,
then trying to grep a local file fails
